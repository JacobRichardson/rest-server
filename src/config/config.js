/** 
 * This is the config file. This file will 
 * contain all configurable values.
 */

// The config object.
const config = {

	database: {
		connectionString: ''
	}
};

// Export the config object.
module.exports = config;